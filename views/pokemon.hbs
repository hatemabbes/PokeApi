<h1> {{capitalize pokemon.name}} </h1>
<img src="{{pokemon.sprites.front_default}}" alt="{{pokemon.name}}">
<h2> Types(s) : </h2>

<ul> 
{{#each pokemon.types}} 
 <li>  {{this.slot}}  : {{capitalize this.type.name}}  </li>
{{/each}}
</ul>